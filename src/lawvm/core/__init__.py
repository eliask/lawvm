"""LawVM core package.

API tier
--------
This package does not define a stable barrel-export surface. Import directly
from concrete submodules so the boundary stays explicit and the exported
surface stays narrow.
"""
